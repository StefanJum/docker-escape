# SYS\_ADMIN

To start the container, run `bash run-docker.sh`.
In the container, look inside the `/host/dev` directory and look for drive partitions.
Mount them using the `mount` command.
You will eventually mount the host filesystem.
